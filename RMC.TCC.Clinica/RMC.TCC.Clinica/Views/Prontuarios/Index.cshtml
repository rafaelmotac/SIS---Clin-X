@model IEnumerable<RMC.TCC.Clinica.Models.Prontuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Cadastrar Novo Prontuário", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.procedimentos)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prescricoes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.historico)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.procedimentos)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prescricoes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.historico)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.paciente_IdPaciente }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.paciente_IdPaciente }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.paciente_IdPaciente })
        </td>
    </tr>
}

</table>
